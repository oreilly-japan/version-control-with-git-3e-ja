## ======================================================================
$ sudo apt install git

# UbuntuのLTS版で最新バージョンのGitをインストールする場合
$ sudo add-apt-repository ppa:git-core/ppa
$ sudo apt install git
